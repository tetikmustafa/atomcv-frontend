/**
 * GENERATED FILE — DO NOT EDIT BY HAND.
 *
 * Produced from the backend's OpenAPI schema:
 *
 *   npm run gen:api      # backend running at localhost:8080
 *   npm run gen:api:ci   # from a committed ./openapi.json
 *
 * This placeholder exists because the backend does not publish a schema yet.
 * It is committed so the slot is visible and so tooling that references the
 * path resolves. The first successful `gen:api` replaces the whole file.
 *
 * Until then, mock handlers are typed by `src/mocks/contracts.ts`, which is
 * scaffolding with a defined end date. Nothing in `src/` outside `src/mocks/`
 * may hand-write a backend-shaped type (CLAUDE.md rule 2).
 */

export type paths = Record<string, never>;
export type components = Record<string, never>;
export type operations = Record<string, never>;
